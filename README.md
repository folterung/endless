# Endless

This sample project demonstrates my ability to develop using React.

## Install Dependencies

Run `npm install`.

## Run locally

Run `npm start`.

## Testing

Run `npm test`.

## Production build

Run `npm run build`.
